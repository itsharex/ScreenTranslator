代码量较大，为了确保准确性和可读性，将分 **4次** 输出。

**本次为第 1 次输出**，包含核心配置文件和构建脚本。这些文件定义了 Tauri v2 的项目结构、依赖关系以及全新的权限系统（ACL）。

### 修改清单 (Part 1 - 配置与依赖)
1.  **`package.json`**: 升级 CLI 和 API 依赖到 v2 版本。
2.  **`src-tauri/Cargo.toml`**: 升级 Rust 依赖 crate 到 v2 版本，添加必要的插件 crate。
3.  **`src-tauri/build.rs`**: 适配 v2 构建脚本。
4.  **`src-tauri/tauri.conf.json`**: 迁移至 v2 配置结构，启用全局 API 兼容。
5.  **`src-tauri/capabilities/default.json`**: **(新增)** v2 核心权限配置文件，替代旧版 allowlist。

---

## 📄 文件: package.json

```json
{
  "name": "screen-translator",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-autostart": "^2.0.0",
    "@tauri-apps/plugin-clipboard-manager": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0",
    "@tauri-apps/plugin-os": "^2.0.0",
    "@tauri-apps/plugin-process": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-window-state": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
```

---

## 📄 文件: src-tauri/Cargo.toml

```toml
[package]
name = "screen-translator"
version = "0.3.0"
description = "A Tauri-based screenshot translator"
authors = ["cc"]
license = "MIT"
repository = "https://github.com/git-hub-cc/ScreenTranslator"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
# Tauri 核心库 v2
tauri = { version = "2.0", features = ["protocol-asset"] }

# v2 官方插件列表
tauri-plugin-shell = "2.0"
tauri-plugin-fs = "2.0"
tauri-plugin-global-shortcut = "2.0"
tauri-plugin-notification = "2.0"
tauri-plugin-clipboard-manager = "2.0"
tauri-plugin-process = "2.0"
tauri-plugin-autostart = "2.0"
tauri-plugin-os = "2.0"
tauri-plugin-window-state = "2.0"

# 通用 Rust 依赖
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
image = "0.24"
arboard = "3"
once_cell = "1.19"
async-trait = "0.1.89"
base64 = "0.22"
encoding_rs = "0.8"
screenshots = "0.6"
```

---

## 📄 文件: src-tauri/build.rs

```rust
fn main() {
    tauri_build::build()
}
```

---

## 📄 文件: src-tauri/tauri.conf.json

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "ScreenTranslator",
  "version": "0.3.0",
  "identifier": "com.tauri.screentranslator",
  "build": {
    "beforeDevCommand": "",
    "beforeBuildCommand": "",
    "devUrl": "../src",
    "frontendDist": "../src"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "title": "ScreenTranslator 设置",
        "label": "main",
        "width": 550,
        "height": 800,
        "resizable": false,
        "fullscreen": false,
        "visible": true
      },
      {
        "label": "screenshot",
        "url": "screenshot.html",
        "fullscreen": true,
        "decorations": false,
        "transparent": true,
        "resizable": false,
        "visible": false,
        "skipTaskbar": true
      },
      {
        "label": "results",
        "url": "results.html",
        "width": 500,
        "height": 700,
        "decorations": false,
        "alwaysOnTop": true,
        "visible": false,
        "skipTaskbar": true
      },
      {
        "label": "image_viewer",
        "url": "image_viewer.html",
        "title": "截图预览",
        "width": 600,
        "height": 400,
        "decorations": false,
        "transparent": true,
        "resizable": true,
        "alwaysOnTop": true,
        "visible": false,
        "skipTaskbar": true
      }
    ]
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "resources": [
      "external/**"
    ],
    "windows": {
      "nsis": {
        "installMode": "currentUser"
      }
    }
  },
  "plugins": {
    "autostart": {
      "args": ["--hidden"]
    }
  }
}
```

---

## 📄 文件: src-tauri/capabilities/default.json

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the app",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-center",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-set-always-on-top",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:path:default",
    "core:event:default",
    "shell:allow-open",
    "shell:allow-execute",
    "fs:default",
    "fs:allow-exists",
    "fs:allow-read-text",
    "fs:allow-write-text",
    "fs:allow-mkdir",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "global-shortcut:allow-is-registered",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "notification:default",
    "clipboard-manager:default",
    "process:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
```